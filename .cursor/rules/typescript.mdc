---
description: TypeScript 与 Vue script 约定
globs: "**/*.{ts,vue}"
alwaysApply: false
---

# TypeScript

## 类型

- 共享类型：`src/types/blog.ts`（`Post`、`Profile`、`PaginatedResult` 等）
- 禁止使用 `as any` 消除报错；应补全类型或收窄

## 错误处理

- 默认不用 `try-catch`；API 层让 Supabase `error` 向上抛，由调用方展示
- 仅在必须吞掉 UI 边界错误（如剪贴板不可用）时局部使用

## 配置函数命名

- 开关：`checkBlogMockSupport()`、`checkSupabaseConfigured()`
- 读取：`getSupabaseUrl()`、`getOwnerGithubId()` 等（`src/config/env.ts`）

## Vue

- 新页面用 `<script setup lang="ts">`
- 组合式逻辑放 `stores/` 或组件内，避免无意义的单行 composable 包装

## 路径别名

`@/` → `src/`（`vite.config.ts` + `tsconfig.app.json`）
