---
description: 认证、Owner 守卫与 GitHub Pages 部署
globs: "src/stores/**/*,src/router/**/*,src/config/**/*,.github/**/*"
alwaysApply: false
---

# 认证与部署

## GitHub OAuth

- 登录：`authStore.signInWithGitHub()` → Supabase `signInWithOAuth({ provider: 'github' })`
- `redirectTo`：`${origin}${import.meta.env.BASE_URL}`（末尾带 `/my-blog/`）

## Owner（前后端双校验）

- 前端：`VITE_OWNER_GITHUB_ID` 或 `VITE_OWNER_GITHUB_LOGIN`（`src/config/env.ts`）
- 路由：`meta.requiresOwner` → `/admin/*`（见 `src/router/index.ts`）
- 后端：RLS `is_owner()`，禁止仅隐藏按钮而不改策略

## 环境变量（`VITE_*`）

| 变量 | 用途 |
|------|------|
| `VITE_SUPABASE_URL` / `VITE_SUPABASE_ANON_KEY` | Supabase |
| `VITE_OWNER_GITHUB_ID` | 博主 GitHub 数字 ID |
| `VITE_OWNER_GITHUB_LOGIN` | 登录名兜底（默认 alone-fool） |
| `VITE_SITE_URL` | 分享链接（生产示例见 README） |
| `VITE_USE_MOCK_BLOG` | 仅 DEV Mock |

Secrets 配在 GitHub Actions；本地用 `.env.development`（不提交 `.env`）。

## GitHub Pages

- `vite.config.ts`：`base: '/my-blog/'` 与仓库名一致
- 改 `base` 须同步：Supabase Redirect URLs、Pages 路径、`VITE_SITE_URL`
- Workflow：`.github/workflows/deploy.yml`
